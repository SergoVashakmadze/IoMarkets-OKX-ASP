node_modules/
.env
*.log
dist/
.DS_Store
pnpm-lock.yaml.bak
# local agent / session tooling (not app code)
.agents/
.claude/
skills-lock.json

# demo artefacts (regenerated by scripts/demo-run.sh — real data, not fixtures)
attestation.json
forged.json
*.mp4

# generated from demo-90s.mp4 by ffmpeg — see docs/SUBMISSION_ANSWERS.md
submission-images/
pitch-slides/

# Local-only audit notes (balances, personal wallets) — never commit
docs/*.local.md
